import Logo from "../assets/logo-nc-news.png";

const Header = () => {
  return (
    <header>
      <div>
        <img src={Logo} alt="nc news logo" />
      </div>
    </header>
  );
};

export default Header;
